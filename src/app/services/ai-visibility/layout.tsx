import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "AI Visibility & Discoverability Services | Brand Iron",
  description: "Improve how AI platforms and search engines discover your business. Brand Iron's AI visibility services combine SEO, AEO, and semantic search strategy.",
};

export default function AIVisibilityLayout({ children }: { children: React.ReactNode }) {
  return children;
}
