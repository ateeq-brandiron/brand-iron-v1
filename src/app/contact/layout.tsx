import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact Brand Iron | Book a Strategy Session",
  description: "Get in touch with Brand Iron's growth marketing team. Book a strategy session to discuss brand strategy, GTM, AI visibility, or website development.",
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children;
}
